apiVersion: v1
kind: Service
metadata:
  name: #{SERVICE_NAME}#
spec:
  type: ClusterIP
  ports:
    - name: http
      port: #{CONTAINER_PORT}#
      protocol: TCP
      targetPort: #{CONTAINER_PORT}#
  selector:
    app: #{APP_NAME}#